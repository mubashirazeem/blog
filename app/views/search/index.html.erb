<div style= "background-image: url('https://mdbcdn.b-cdn.net/img/Photos/new-templates/search-box/img4.webp');">
<h1 class="text-center"><%= pluralize(@blog.count, "Result") %></h1>

<div class="container">
  <table class="table table-striped">
    <thead>
      <tr>
        <th><%= sort_link(@query, :title, "Title", default_order: :asc) %></th>
        <th><%= sort_link(@query, :description, "Blog description") %></th>
      </tr>
    </thead>
    <tbody>
      <% @blog.each do |blog| %>
        <tr>
          <td><%= link_to blog.title, blog %></td>
          <td><%= blog.description %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
</div>